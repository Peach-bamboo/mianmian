# ADR-001：题库采用云端优先、本地兜底

- 状态：已采用
- 日期：2026-06

## 背景

本地 JSON 无法支持后台动态发布；直接依赖云端又会在弱网、未部署或空数据库时造成空白页面。

## 决策

页面统一通过题库服务层读取数据：

```text
缓存可用 -> 返回缓存
否则请求云端 -> 成功后写缓存
云端失败或未初始化 -> 使用内置题库
```

## 结果

- 页面与数据源解耦。
- 内容可动态更新。
- 小程序保留离线可用性。
- 服务层和缓存逻辑复杂度增加，需要测试保护。
